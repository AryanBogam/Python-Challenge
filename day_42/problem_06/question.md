Q6. Extension List Checker
Task: Given a list of installed extensions, check if a certain extension (e.g., "Python") is installed.
Example:

["Python", "Prettier", "Live Server"] → "Python" installed 
Why: Simulates the extension manager.