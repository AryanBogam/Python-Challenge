Q8. LFU Cache (Medium-Hard)

Problem: Design LFU (Least Frequently Used) cache with:
get(key)
put(key, value)

Unlike LRU, eviction is based on least frequency of usage.
Hint: Use HashMap + min frequency tracking.