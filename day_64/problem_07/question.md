Q7. Check for Balanced Parentheses

Problem:
Given a string with parentheses ()[]{}, check if they are balanced.
Example: "([{}])"→ True, "([)]" → False
Concepts: Stacks, strings, loops.
Hint: Use a stack (list append/pop) for open/close pairs.