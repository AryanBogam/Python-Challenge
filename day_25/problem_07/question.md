Q7. Multi-Error Handler
Problem:
Create a calculator that handles:
Division by zero
Invalid numbers (like "abc")
Operation errors (like using % or ^ which are not supported)
Goal: Use try-except with multiple exception types.